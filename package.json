{
  "name": "restaurant-pos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open": "npx cap open android",
    "cap:run": "npm run build && npx cap copy && npx cap sync",
    "test": "node --test server/test.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.3.4",
    "@element-plus/icons-vue": "^2.3.1",
    "echarts": "^5.5.0",
    "element-plus": "^2.7.0",
    "vue": "^3.4.0",
    "vue-echarts": "^7.0.3",
    "vue-router": "^4.3.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
